source "https://rubygems.org"

gem "jekyll", "~> 4.3"

# Minimal Mistakes wird per remote_theme geladen (kein lokaler Gem nötig)
group :jekyll_plugins do
  gem "jekyll-include-cache"
  gem "jekyll-feed"
  gem "jekyll-sitemap"
end
